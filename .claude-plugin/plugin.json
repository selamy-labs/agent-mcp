{
  "name": "agent-mcp",
  "version": "0.1.0",
  "description": "Selamy Labs public MCP marketplace-aggregator. Bundles the per-server MCP packages (laneq, reddit, dispatch, memory, telemetry) by pinned tag for reproducible installs; each server is maintained in its own repo.",
  "mcpServers": "./mcp-servers.json"
}
